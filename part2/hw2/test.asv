A1 = [1 3 2; 4 2 5; ]
A1
size(A1)

x1 = [1 1 2]'
x1
size(x1)

sprintf('A1 * x1 = ')
A1 * x1

A2 = [1 1 2; 4 3 5; ]
A2
size(A2)

x2 = [1 2 2]'
x2
size(x2)

sprintf('A2 * x2 = ')
A2 * x2

A = horzcat(A1, A2)
x = vertcat(x1, x2)

sprintf('A * x = ')
A * x

cvx_begin
    variable t(3, 2)
    y = reshape(x, [3, 2])
    
    y1 = y.^2
    y2 = sum(y1, 1)
    y3 = y2.^(1/2)
    y4 = sum(y3)
    z = sum((sum((reshape(x, [3, 2])).^2, 1)).^(1/2))
    % t = y
    variavle a0
    for k in 1:2
        a0 = a0 + norm(t(:, k))
    end
    a1 = norm(t, 2)
cvx_end

